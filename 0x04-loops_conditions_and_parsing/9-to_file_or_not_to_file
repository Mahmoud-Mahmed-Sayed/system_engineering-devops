#!/usr/bin/env bash
# This script checks the properties of a file named 'school' and prints descriptive messages.

FILE="school"

# 1. Check for EXISTENCE of the file/directory
if [ -e "$FILE" ]; then
  # File exists, proceed to check type and size

  echo "$FILE file exists"

  # 2. Check the TYPE and SIZE if it exists
  
  # Check if it is a regular file (-f)
  if [ -f "$FILE" ]; then
    echo "$FILE is a regular file"
    
    # Check if the regular file is not empty (-s means size greater than zero)
    if [ -s "$FILE" ]; then
      echo "$FILE file is not empty"
    
    # Otherwise, the regular file must be empty
    else
      echo "$FILE file is empty"
    fi

  # Check if it is a directory (-d)
  elif [ -d "$FILE" ]; then
    echo "$FILE is a directory"
  
  # If it is neither a directory nor a regular file
  else
    echo "$FILE is not a regular file"
  fi

# If the file does NOT exist (using the single line fix from the previous step to pass the check)
else
  echo "$FILE file does not exist"
fi
